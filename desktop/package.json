{
  "name": "chemviewer-desktop",
  "version": "0.2.0",
  "private": true,
  "description": "ChemViewer desktop shell (Electron) — from-source dev launcher + packaged-app (DMG) builder",
  "author": "yjzhng",
  "main": "electron/main.cjs",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "dist": "node scripts/build-dmg.mjs",
    "builder": "electron-builder --config electron-builder.cjs"
  },
  "dependencies": {
    "@duckdb/node-api": "^1.5.4-r.1"
  },
  "devDependencies": {
    "electron": "^33.4.11",
    "electron-builder": "^25.1.8"
  }
}
